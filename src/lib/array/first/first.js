/**
 * first()
 * ----------
 * Method that return the first item in an array.
 * @param {array} array - An array.
 * @return {any} - The first element.
 */

const first = array => array[0]

export default first
