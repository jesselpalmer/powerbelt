/**
 * last()
 * ----------
 * Method that return the last item in an array.
 *
 * @param {array} array - An array.
 * @return {any} - The last element.
 */

const last = array => array[array.length - 1]

export default last
