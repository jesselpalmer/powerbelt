export const ___ = {
    isBoolean: str => {
        return typeof str === 'boolean';
    },

    isNumber: num => {
        return typeof num === 'number';
    },

    isString: str => {
        return typeof str === 'string';
    }
};